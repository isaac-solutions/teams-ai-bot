# Core Python dependencies
pymongo==4.6.0
motor==3.3.2

# Azure services
azure-servicebus==7.11.4
azure-storage-blob==12.19.0

# HTTP client for downloading files
httpx==0.25.2
aiohttp>=3.9.0  # Required for async Azure Blob Storage transport

# Environment and configuration
python-dotenv==1.0.0

# Docling for document conversion to markdown
docling>=2.60.0
docling-core>=2.0.0

# OpenAI for embeddings (Azure OpenAI compatible)
openai==1.54.3

# Text processing and chunking
tiktoken==0.7.0
langchain-text-splitters==0.3.0

# Image processing (required by docling)
Pillow>=10.0.0

# Note: bson is included with pymongo, no separate package needed